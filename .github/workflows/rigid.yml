name: blank

on:
  push:

jobs:
  correction:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        read: ["involvement", "differ", "fortunately", "exclusive", "threaten"]
        accuse: ["poem", "distribute", "valid", "ridge"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Fr
